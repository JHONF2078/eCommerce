{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    //para conectarse desde local a la bd del contenedor  Host=localhost;
    //para conectarse desde el contenedor a la bd del  Host=postgres-container;
    "PostgresConnection": "Host=postgres-container;Port=5432;Username=postgres;Database=eCommerceUsers"
  }
}
